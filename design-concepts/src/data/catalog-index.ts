import index from './generated/catalog-index.json'

export const inoplexCatalogSlugs = index.inoplex.bySubcategory
export const INOPLEX_SLUGS: string[] = index.inoplex.slugs

export const zanoCatalogSlugs = index.zano.bySubcategory
export const ZANO_SLUGS: string[] = index.zano.slugs
export const ZANO_HIDDEN_SLUGS: string[] = index.zano.hiddenSlugs

export const streetparkCatalogSlugs = index.streetpark.bySubcategory
export const STREETPARK_SLUGS: string[] = index.streetpark.slugs

export const investimCatalogSlugs = index.investim.bySubcategory
export const INVESTIM_SLUGS: string[] = index.investim.slugs

export const vvzPlayCatalogSlugs = index.vvzPlay.bySubcategory
export const VVZ_PLAY_SLUGS: string[] = index.vvzPlay.slugs

export const novumCatalogSlugs = index.novum.bySubcategory
export const NOVUM_SLUGS: string[] = index.novum.slugs

export const kuschCatalogSlugs = index.kusch.bySubcategory
export const KUSCH_VCARE_FOLD_SLUGS: string[] = index.kusch.slugs

export const avfallCatalogSlugs = index.avfall.bySubcategory
export const BINSIGNIA_REST_SLUGS: string[] = index.avfall.slugs
